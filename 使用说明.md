# 公交车辆图片分类工具使用说明

## 功能介绍
该工具用于对公交车辆内部图片进行分类，主要包括两步分类：
1. **第一步分类**：区域分类（前车厢、后车厢、站立禁区、都不是）
2. **第二步分类**：拥挤度分类（empty、seated、standing、crowd、extremecrowd）

## 运行方法
1. 确保已安装Python 3和所需依赖（tkinter、PIL）
2. 双击运行 `image_classifier.py` 文件
3. 或在命令行中执行：`python image_classifier.py`

## 界面说明
程序启动后会显示一个窗口，包含以下部分：

### 1. 分类状态显示
显示当前分类进度和状态信息

### 2. 第一步分类：区域
包含4个按钮：
- 前车厢
- 后车厢  
- 站立禁区
- 都不是

### 3. 第二步分类：拥挤度
包含5个按钮：
- empty（空车）
- seated（坐着的人）
- standing（站着的人）
- crowd（拥挤）
- extremecrowd（极度拥挤）

### 4. 控制按钮
- 下一张：完成分类后点击
- 跳过：跳过当前图片

### 5. 图片显示区域
显示当前待分类的图片

## 操作步骤
1. 程序启动后会自动加载 `collected` 文件夹中的图片
2. 对于每张图片：
   - 首先选择区域分类（第一步）
   - 然后选择拥挤度分类（第二步）
   - 点击「下一张」按钮完成分类
   - 或点击「跳过」按钮跳过当前图片
3. 分类完成的图片会被移动到 `sorted` 文件夹中，并按照分类结果组织
4. 分类记录会保存在 `classified_images.json` 文件中，避免重复分类

## 注意事项
- 确保 `collected` 文件夹中有待分类的图片
- 程序会自动处理子文件夹中的图片
- 分类完成后，图片会按照「区域_拥挤度_原始名称」的格式重命名
- 如果窗口显示不全，可以拖动窗口边缘调整大小

## 示例
分类完成后，图片会被组织到类似以下结构：
```
sorted/
├── front/
│   ├── empty/
│   ├── seated/
│   ├── standing/
│   ├── crowd/
│   └── extremecrowd/
├── rear/
│   ├── empty/
│   ├── seated/
│   ├── standing/
│   ├── crowd/
│   └── extremecrowd/
├── standing/
│   ├── empty/
│   ├── seated/
│   ├── standing/
│   ├── crowd/
│   └── extremecrowd/
└── other/
    ├── empty/
    ├── seated/
    ├── standing/
    ├── crowd/
    └── extremecrowd/
```
